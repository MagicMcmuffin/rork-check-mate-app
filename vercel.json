{
  "version": 2,
  "builds": [
    {
      "src": "backend/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/hono.ts"
    },
    {
      "src": "/(.*)",
      "dest": "backend/hono.ts"
    }
  ]
}
